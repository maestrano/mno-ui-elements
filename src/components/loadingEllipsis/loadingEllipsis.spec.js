//var app = angular.module('mnoUiElements', []);

describe('Component: loadingEllipsis', function() {

  // 1. Load the module
  beforeEach(module('mnoUiElements'));
  //
  // // 2. Inject $rootScope and $compile
  // var scope, element;
  // beforeEach(inject(function($rootScope, $compile) {
  //   scope = $rootScope.$new();
  //   element = angular.element('<mno-loading-ellipsis></mno-loading-ellipsis>');
  //   element = $compile(element)(scope);
  //   scope.outside = '1.5';
  //   scope.$apply();
  // }));

  it('Should create three ellipsis', function() {
    //
    // var final = '<div class="mno-three-bounce">' +
    //   '<div class="mno-bounce1"></div>' +
    //   '<div class="mno-bounce2"></div>' +
    //   '<div class="mno-bounce3"></div>' +
    // '</div>';
    //
    // expect(element.text()).toEqual(final);
  });
});
